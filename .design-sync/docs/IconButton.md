---
category: Actions
---

# IconButton

A square 44px control for icon-only actions - reorder, remove, dismiss - where a label would crowd the row. Always give it an `aria-label`. `tone="danger"` tints the hover state for destructive row actions.
