---
category: Forms
---

# Input

The single-line text control. Near-opaque by design: recipes are read while cooking, and text over the app's moving mesh is not legible. Pair with `FieldLabel`, and point `aria-describedby` at a `FieldHint` or `FieldError`.
