---
category: Actions
---

# ButtonLink

A Button that navigates instead of acting. Identical styling to `Button` - same variants and sizes - but renders a react-router `<Link>`, so use it whenever the result is a route change rather than a mutation.
