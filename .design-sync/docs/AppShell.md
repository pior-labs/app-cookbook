---
category: Shell
---

# AppShell

The authenticated app frame: the rail, the mobile nav, the account menu, and the atmosphere. It renders a react-router `<Outlet />` for the active screen, so it is used as a layout route rather than composed by hand.
