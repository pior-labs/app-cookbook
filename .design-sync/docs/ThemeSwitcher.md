---
category: Theming
---

# ThemeSwitcher

The theme list, as used inside the account menu. Renders every theme in `THEMES` with its swatch and a check on the active one. Pass `onSelect` to close the containing menu after a choice.
